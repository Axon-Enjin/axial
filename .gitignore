# Cursor / agent skills (local tooling — not product code)
.agents/
.cursor/*
!.cursor/mcp.json
!.cursor/README.md
!.mcp.json
.gemini/
skills-lock.json

# Secrets — never commit
.env*
**/.env*
!**/*.example
!**/*.example.json

# Soroban local + deployment overrides (team commits *.example.json only)
soroban/deployments/testnet.json
soroban/deployments/mainnet.json
soroban/target/
.soroban/
.stellar/

# Node & Vercel
node_modules/
.next/
out/
.vercel/

# Ad-hoc local scripts
derive.js

# Internal working directories (not product code)
claude-design/
.tmp/

# Pitch Deck PDF (HTML is committed, PDF is generated locally)
docs/pitch-deck.pdf

# Utility scripts — ignore local node_modules
scripts/**/node_modules/

# OS
.DS_Store
Thumbs.db

# Editors & IDEs
*.swp
*.swo
*~
.idea/
.vscode/
